# Домашка по компиляторам

## Структура

В отдельных директориях находятся реализации компиляторов.
Все компилятора объединены в один dune project, на каждый компилятор должен быть отдельный dune package.
Тестироваться будет вот так: `dune b -p demo` или `dune test -p demo` (при условии, что demo --- название компилятора).

Все компиляторы должны запускаться с помощью команды `dune exec demo/compiler.exe`.

### Необходимые опции командной строки

У всех будут требования на опции командной строки (напишу потом).

## Задания

Для получения допуска нужно будет сделать вовремя N заданий. Точное распрередение будет позже.

Разбейтесь по группам, запишитесь в teams.yml.
Разрабатывайте компилятор в своей директории.
В CI будет использоваться OCaml 5.5.1.

### Задание 1. Фронт

Реализуйте синтаксические анализаторы для подмножетсва OCaml

* досточного, чтобы реализовать CPSные факториал и фибоначчи;
  * нисходящий (комбинаторы или рекурсивный спуск, на выбор)
  * восходящий (menhir + ocamllex)
  * можно будет отключить проверку присваивания у линтера Zanuda, теперь он умеет отключать анализы с точностью до модуля.
* сделайте quickcheck тестирование;
* эксперимент по сравнению производительности двух парсеров;
* Чтобы показать, что menhir не очень, реализуйте в нисходящем парсере (на выбор)
  * идентификаторы, слипшиеся с ключевыми словами.
  * переопределние арности/ассоциативности операторов.

  Дополнительный синтаксис должен включаться явно опцией командной строки.
* если что-то не понятно --- спросите нейронку.
* дедлайн 26 сентября, 15:00.

### Задание 2. Вывод типов

### Задание 3. RISC-V